# Solana Trading Bot Configuration

# Required: Your wallet's private key (supports two formats)
# Format 1: Comma-separated byte array
# PRIVATE_KEY=150,92,38,155,143,205,113,129,...
# Format 2: Base58 string  
# PRIVATE_KEY=4YDtkEXhXgfgfhjjhkjjkjkjbbbffgfghhhyyyjjjgfdddssweeerrfgggKeekira
PRIVATE_KEY=REPLACE_WITH_YOUR_ACTUAL_PRIVATE_KEY

# Required: Token address to trade
TOKEN_ADDRESS=REPLACE_WITH_YOUR_TOKEN_ADDRESS

# Optional: Solana RPC endpoint (defaults to mainnet)
RPC_URL=https://api.mainnet-beta.solana.com

# Optional: Trading amount range in SOL
SOL_AMOUNT_MIN=0.001
SOL_AMOUNT_MAX=0.01

# Optional: Interval between trades in seconds
TRADE_INTERVAL_MIN=30
TRADE_INTERVAL_MAX=300

# Optional: Maximum slippage tolerance (5% = 0.05)
MAX_SLIPPAGE=0.05

# Optional: Minimum SOL balance to maintain
MIN_SOL_BALANCE=0.1

# Optional: Buy percentage (60 = 60% buy, 40% sell)
BUY_PERCENTAGE=80